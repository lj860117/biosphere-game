---
name: hermanlei-conventions
description: >
  厉害了哥（hermanlei）的个人开发习惯、代码规范和工作流约定。
  hermanlei 主要从事 Unity 编辑器工具开发和渲染方向的工作。
  涵盖代码重构纪律、Unity 编辑器工具开发偏好（UI排版布局、颜色编码、分步引导、
  列表/树形展示、连接/登录流程、按钮交互模式）、功能模块间接口设计、
  Git 版本管理规范、P4 工作流、以及 UI/UX 设计原则。
  当为 hermanlei 编写代码、做重构、开发 Unity 工具、开发渲染/Shader 相关功能、
  或管理版本控制时，必须遵守这些约定。
  This skill should be used when working on any coding task, code review,
  refactoring, Unity editor tool development, rendering/shader development,
  or version control operations for hermanlei's projects.
---

# 厉害了哥的开发约定 (hermanlei-conventions)

本 Skill 定义了 hermanlei（厉害了哥）在日常开发中积累的核心习惯和偏好。
所有协助 hermanlei 的工作都必须遵守这些约定，它们来自真实项目中的血泪教训。

## 适用范围

hermanlei 当前的主要工作方向：

- **Unity 编辑器工具开发** — 自定义 EditorWindow、PropertyDrawer、AssetPostprocessor 等，代表作品为 SceneCraft（场景工坊）工具套件
- **渲染方向** — 图形渲染管线、Shader 开发、材质系统、渲染优化
- **P4 (Perforce) 集成** — 在 Unity 编辑器内集成 P4 版本控制操作

本约定中的 UI 设计偏好、按钮交互模式、颜色编码等规则，均来自上述方向的实际开发经验。
**当协助 hermanlei 开发相关工具时，必须加载并遵守本 Skill。**

---

## 设计哲学（一切规则的"为什么"）

> **一句话版**：用机制代替人力，用数据驱动代替硬编码，用小步验证代替大步翻车。

这个 Skill 里所有的铁律、规范、流程，都围绕**三个核心信念**：

1. **一个人 + AI 也能做出专业团队的质量** — 前提是用机制（闭环流程、检查清单、自动巡检）替代"有人审"。规则存在不是为了限制你，而是替你盯着那些容易忘的事。

2. **踩过的坑是最贵的知识，必须留住** — 每条铁律背后都是一次真实事故（正则重构炸 7 个文件、P4 没备份回不去…）。踩坑 → 记录 → 变成规则 → 下次不再踩。这就是这个 Skill 的生长方式。

3. **细节决定成败，但不是所有细节都同等重要** — 所以有温度分级（🔴铁律 > 🟡规范 > 🟢知识）、有任务分级（🟢轻量 / 🟡标准 / 🔴重度）。该严的地方寸步不让，该快的地方不拖泥带水。

> _把规则内化为习惯，习惯就不再是负担。_

---

## Skill 元规范（读我优先！）

> **本节是整个 Skill 的"使用手册"——任何 AI、任何模型、任何平台，拿到这个 Skill 后先读这里。**
> 如果你是第一次加载这个 Skill，花 30 秒读完本节就知道怎么用了。

### 这个 Skill 是什么

这不是一份静态文档，而是一个**活的知识系统**。它有：
- **规则**（SKILL.md 主文件）：必须遵守的行为准则
- **工具箱**（references/ 目录）：按需查阅的知识文档
- **自我进化机制**（成长引擎）：定期更新自己

### 快速上手：拿到 Skill 后的第一步

```
1. 读「内容分层体系」→ 知道哪些是铁律（🔴必须遵守）哪些是参考（🟢按需查阅）
2. 读「项目识别索引」（第六章）→ 知道用户在说哪个项目
3. 读「Skill 自动路由规则」→ 知道需不需要叠加其他 Skill
4. 开始干活时，按「工具开发闭环流程」（第零章）走
5. 碰到具体领域问题 → 查 references/ 目录下对应的知识索引
```

### 内容地图（做什么事读哪些章节）

> **不需要每次全读 1200+ 行**。根据当前任务，只读相关章节即可。

| 你在做什么 | 必读 | 建议读 | 不需要读 |
|-----------|------|--------|---------|
| **写新 Unity 编辑器工具** | 零（闭环流程）、二（编辑器偏好）、三（接口设计） | 七（C# 编码规范）、八（SceneCraft：只需读🔴铁律 + 8.4/8.7/8.8，其余按需查） | 五（P4）、渲染知识索引 |
| **重构/改老代码** | 一（重构铁律，🔴 必读！）、七（编码规范） | 零的④增量实施、八的🔴铁律（如果是 SceneCraft） | 二（编辑器偏好）、渲染知识索引 |
| **渲染/Shader 开发** | 一（重构铁律）、七（编码规范） | `references/rendering-knowledge-index.md` | 二（编辑器偏好）、八（SceneCraft） |
| **Git/版本管理操作** | 四（Git 规范） | 五（如果涉及 P4） | 其他所有 |
| **做游戏（biosphere-game）** | 一（重构铁律）、七（编码规范 + 7.9 游戏特有规范） | `references/gamedev-knowledge-index.md` | 二（编辑器偏好）、八（SceneCraft） |
| **前沿调研/巡检** | Skill 成长引擎 | `references/README.md` | 编码相关章节 |
| **聊天/讨论/设计** | 九（沟通风格） | 六（项目约定，需要知道项目路径时） | 编码相关章节 |

> **SOUL.md vs SKILL.md 的分工**：
> - `SOUL.md` = 人格底线 + 铁律摘要（不管有没有加载 Skill 都生效的最低保障）
> - `SKILL.md` = 铁律详细说明 + 工作流 + 专业知识 + 成长系统
> - 两者有重叠是**有意为之**——Skill 没加载时，SOUL.md 是唯一防线

### 跨平台可移植性

本 Skill 设计为**平台无关、模型无关**：

| 场景 | 怎么做 |
|------|--------|
| 换了 AI 模型（Claude → GPT → Gemini） | Skill 文件照搬，任何模型都能读懂 Markdown |
| 换了开发平台（WorkBuddy → Cursor → Windsurf） | 只要支持加载 .md 文件，就能用 |
| 换了电脑（办公室 → 家里） | 通过 Git 仓库同步（见「快捷指令：更新 git」） |
| 多人协作（目前只有一个人） | Skill 本身是个 Git 仓库，天然支持多人 |

**关键设计原则**：
1. **纯 Markdown，零依赖** — 不依赖任何特定工具或 API
2. **自描述** — 每个文档开头说明用途和维护规则，不需要额外的"说明书"
3. **目录即索引** — `references/README.md` 是知识文档的索引，SKILL.md 的「参考文档」章节是入口

### 内容的四种"温度"

> 解决"这么多内容怎么区分、什么时候更新什么"的问题。

| 温度 | 标签 | 什么内容 | AI 的态度 | 更新节奏 | 迁移方向 |
|------|------|----------|----------|----------|----------|
| 🔥 **烫的** | 🔴 铁律 | 重构铁律、备份检查单、安全重构四件套 | **绝对不能违反** | 踩坑后才更新 | 几乎不变 |
| 🌡️ **暖的** | 🟡 规范 | 编码规范、闭环流程、架构原则、命名规范 | **应该遵守** | 发现更好实践时更新 | 好习惯可以升温为铁律 |
| 🧊 **凉的** | 🟢 知识 | 渲染索引、技术选型、前沿调研 | **按需参考** | 每月前沿调研 | 反复验证有效可以升温为规范 |
| 💨 **冷的** | ⚪ 约定 | 路径、Git 规范、快捷指令 | **照做就行** | 项目变化时 | 约定就是约定，不升不降 |

**温度迁移规则**：
- 🟢→🟡：某个"参考"级别的实践，在 3 次以上的实际开发中被证明有效 → 升为规范
- 🟡→🔴：某个"规范"级别的规则，违反后**实际造成过事故** → 升为铁律
- 🔴→🟡：极少发生，除非项目技术栈彻底变了导致某条铁律不再适用
- 任何层级→删除：技术过时、项目废弃、被更好的替代

### Skill 使用规范（AI 行为准则）

> 不管是哪个 AI 在用这个 Skill，都必须遵守以下行为准则。

1. **加载 Skill 后先报告**：告诉用户"已加载 hermanlei-conventions Skill"，不需要列出所有规则
2. **铁律不可跳过**：🔴 铁律级别的规则，即使用户说"不用管那个"也要**提醒一次**（提醒后用户仍坚持则执行）
3. **不确定时查文档**：碰到领域问题先查 references/，不要凭空回答
4. **改 Skill 必须告知**：任何对 SKILL.md 或 references/ 的修改，都要列出改了什么并等用户确认
5. **不要过度加载**：一次最多叠加 2 个 Skill（本 Skill + 1 个领域 Skill），多了 AI 上下文会爆
6. **working memory 是桥梁**：对话中的临时发现写入 working memory → 确认有价值后再迁入 Skill

---

## 零、工具开发闭环流程（顶层工作流）

> **核心原则**：不是写完代码就结束，而是 设计→搭骨架→填肉→验证→预审→复盘→更新规则 的完整闭环。
> 每一轮闭环的产出（教训、规则、架构文档）都会喂给下一轮，形成正向飞轮。

### 任务分级（先判断再动手）

> **不是所有任务都需要走完整 8 步闭环。** 先花 5 秒判断任务等级，再决定走哪些步骤。

| 等级 | 判断标准 | 走哪些步骤 | 例子 |
|------|----------|-----------|------|
| 🟢 **轻量** | 改动 ≤ 1 个文件 且 ≤ 30 行，无架构变化 | 直接改 → 验证 → 完事 | 修 typo、调参数、改文案、加注释 |
| 🟡 **标准** | 改动 2~5 个文件 或 30~200 行，单模块内 | ①④⑤⑧（跳过 ②③⑥⑦） | 加小功能、修 bug、小规模重构 |
| 🔴 **重度** | 改动 > 5 个文件 或 > 200 行，涉及架构/跨模块 | **全部 8 步，不跳** | 新模块、大重构、新工具、架构变更 |

**分级规则**：
- **拿不准时按高一级处理** — 宁可多走一步也别漏了关键检查
- **过程中发现低估了 → 随时升级** — 本来以为是 🟡 结果越改越多，立即切换到 🔴 流程
- **🔴 铁律不受分级影响** — 不管什么等级，重构铁律（第一章）和备份检查单都必须遵守

### 8 步闭环

```
① 方案设计 → ② 架构评审 → ③ 程序框架 → ④ 增量实施(+L1守门)
                                                    ↓
⑧ 更新规则 ← ⑦ 复盘总结 ← ⑥ 预审检查 ← ⑤ 落地验证
```

| 步骤 | 名称 | 做什么 | 谁主导 | 产出物 | 跳过条件 |
|------|------|--------|--------|--------|----------|
| ① | **方案设计** | 明确做什么、为什么、不做什么。不需要长篇大论，一段话说清楚即可 | 用户 | 需求描述 | 10 行以下小改可跳过 |
| ② | **架构评审** | AI 出架构方案（文件拆分、类职责、数据流），**用户审** | AI 提案→用户拍板 | 架构文档 / 口头确认 | 单文件改动可跳过 |
| ③ | **程序框架** | 搭空壳：类定义、方法签名、接口、TODO 注释，**不写实现**。确保编译通过、结构合理 | AI | 可编译的骨架代码 | 功能 < 50 行可跳过 |
| ④ | **增量实施** | 一个功能一个功能填肉。每填完一块自动跑 L1 日常守门（见 8.9 节） | AI（用户验收） | 可运行的代码 | — |
| ⑤ | **落地验证** | 用户在 Unity 里实际操作，验功能、验手感 | 用户 | Bug 反馈 / 体验意见 | — |
| ⑥ | **预审检查** | 按 8.9 节的 L2/L3 机制做代码预审，对照台账和规则 | AI | 更新台账 | 小改跟 L1 守门合并 |
| ⑦ | **复盘总结** | 这次踩了什么坑？什么决策对了？什么模式以后要避免？ | 用户 + AI | 复盘记录 | 用户说"不用复盘" |
| ⑧ | **更新规则** | 把复盘结论写回 SKILL.md / 台账 / 架构文档 | AI | 规则迭代 | 没有新发现时跳过 |

### 验证哲学：没有团队，靠机制

> 传统团队每个环节有人审（产品审需求、主程审架构、QA 测功能）。
> 我们是"一个人 + AI"，没有第三方审核者。
>
> **解法不是找人审，而是让每个环节的结果"可被验证"：**
> - 方案设计 → 用挑战性问题逼出答案（有答案 = 被验证）
> - 架构评审 → 用自检清单替代架构师（全打勾 = 通过）
> - 程序框架 → 用编译器验证（编译过 = 结构没问题）
> - 增量实施 → 用编译+运行+规则三层叠加（覆盖面 ≥ Code Review）
> - 落地验证 → 用第①步的验收标准逐条对照（前后呼应，不漏项）
> - 复盘总结 → 用对比法替代主观判断（对照台账/规则/历史，客观事实说话）
>
> **核心思想：把"人审"变成"机制审"，把"拍脑袋觉得行"变成"对照清单全打勾"。**

### 各步骤详细说明

#### ① 方案设计

用户不需要写正式文档，口头描述即可。AI 的职责是**追问关键缺失**：
- 这个功能给谁用？（自己 / 美术 / 策划）
- 预期操作多大数据量？（影响架构选型）
- 有没有现有代码可以复用？（避免重复造轮子）
- 有没有**不要做**的事？（边界比需求更重要）

**验证锚点 — 3 个挑战性问题**（AI 必须问，用户回答后方案才算"过审"）：
1. **这个功能不做会怎样？**（验证必要性——能不做就不做）
2. **有没有更简单的方式达到同样效果？**（验证是否过度设计）
3. **做完之后，你怎么知道它是对的？**（前置定义验收标准——后面第⑤步靠这个验）

> 有答案的问题就是被验证过的问题。不需要别人审，自问自答也比不问强。

**方案规范模板**（不需要写长文档，但至少把这 5 条填了）：

```
【做什么】一句话说清功能目标
【给谁用】自己 / 美术 / 策划 / 所有人
【不做什么】明确的边界（比"做什么"更重要）
【验收标准】做完后怎么知道它是对的（第⑤步回来逐条对照）
【已知风险】可能踩的坑、性能瓶颈、与现有代码的冲突
```

> **这份规范是活文档**——整个开发过程中保持可访问。第④步实施时发现边界变了，回来更新它。第⑤步验收时拿它逐条打勾。不是写完就扔的一次性文档。

#### ② 架构评审

**触发条件**：新工具 / 新模块 / 预计超过 200 行的功能
**AI 必须输出**：
1. 文件列表 + 每个文件的职责（一句话）
2. 类之间的依赖方向（谁引用谁）
3. 数据流：数据从哪来、存在哪、谁读谁写
4. 与现有代码的边界（复用什么、不碰什么）

**关键原则**：
- 单文件不超过 500 行（EditorWindow 工具类）
- 单类单职责，用"和"能描述的就该拆
- P4 操作统一走 P4AssetHelper，不新建第 N 套
- 新建 ScriptableObject 时遵循 `[CreateAssetMenu]` 惯例
- **AI 可读性**：文件名、类名、方法名要让 AI 仅读签名就能理解职责，不依赖内部实现细节。公共方法加一行 `///` XML 注释概述意图

**用户拍板后才动手**。哪怕用户只说"行"一个字，也算确认。

**验证锚点 — 架构自检清单**（AI 出完方案后自己过一遍，把结果展示给用户）：
```
□ 每个文件的职责能用一句话说清吗？（说不清 = 职责不清，拆）
□ 有没有文件会超过 500 行？（超了 = 要拆 partial 或独立文件）
□ 类之间有没有循环依赖？（A→B→A = 架构有环，必须打断）
□ 新增代码有没有和现有代码功能重复？（查台账 + 架构文档）
□ 如果删掉其中一个文件，其他文件能编译过吗？（不能 = 耦合太紧）
□ 非程序员能不能通过 Inspector / 配置文件使用这个功能？
□ AI 能否仅读文件名+类签名就理解模块职责？（不能 = 命名/结构需优化）
```
> 用户不需要看架构本身，只看清单结果。6 个框全打勾 = 架构没大问题。

#### ③ 程序框架

先搭骨架再填肉，好处：
- 编译错误在结构阶段就能发现，不用写了 300 行才发现设计不对
- 用户可以看到"全貌"再决定要不要调整
- 方法签名定下来后，多个方法可以并行实施

**骨架标准**：
- 所有类、方法、字段的定义完整
- 方法体只有 `// TODO: 实现 XXX` 或 `throw new NotImplementedException()`
- 必须能编译通过

**验证锚点**：骨架写完后，请用户在 Unity 里编译一次。编译通过 = 结构没问题。这是最便宜的验证——发现问题只需改接口，不需要改实现。

#### ④ 增量实施

**核心纪律**：
- 一次只做一个功能点，做完告诉用户"XXX 做好了"
- 每完成一个功能自动跑 L1 守门检查（见 8.9 节）
- 发现问题就地修，不攒到后面
- 超出原定架构的需求，先跟用户说"这个超出了原来的设计，要不要调整架构？"

**验证锚点 — 三层叠加**（每完成一个功能块都要过）：
1. **编译通过** — AI 明确说"请编译试一下"，语法错误在这里拦住
2. **用户操作一次** — 功能对不对，用户最清楚
3. **L1 守门自查** — AI 对照规则检查，规范问题在这里拦住

> 三层叠加的覆盖面不比 Code Review 差——Code Review 也看不出运行时 bug。

#### ⑤ 落地验证

AI 没法替用户在 Unity 里操作，但可以：
- 提醒用户"建议测这几个场景"
- 用户报 bug 时，优先看是不是架构问题而非实现 bug

**验证锚点 — 回溯验收标准**：
- 拿出第①步定义的"怎么知道它是对的"，逐条对照
- AI 输出一份简短的验收检查清单（基于①的回答生成），用户逐项打勾
- 全部打勾 = 功能验收通过；有没打勾的 = 记录为 bug 回到第④步

#### ⑥ 预审检查

具体流程见 **8.9 代码预审工作流**。
关键：不是每次都全量审，台账是缓存，用台账省 token。

#### ⑦ 复盘总结

**每个工具 / 大功能做完后，AI 主动问**："要花 2 分钟复盘一下吗？"

复盘模板：
```
## 复盘：[工具名/功能名]（日期）
- ✅ 做对了什么：
- ❌ 踩了什么坑：
- 💡 下次怎么避免：
- 📏 要不要更新规则：
```

用户说"不用"就跳过，但 AI 必须问。

**验证锚点 — 对比法**（避免"你觉得好我也觉得好"的确认偏见）：
- **对照台账**：这次做的东西，有没有犯台账里记录过的**老毛病**？
- **对照规则**：SKILL.md 里的每条相关规则，这次有没有违反的？
- **对照上次**：跟上一个同类功能/工具比，这次哪里更好、哪里更差？

> 不靠主观判断，靠跟历史记录客观对照。数据不会骗人。

#### ⑧ 更新规则

复盘有新发现时，AI 把结论写进：
- **SKILL.md** — 新增/修改具体规则条目
- **台账** — 新增已知问题
- **架构文档** — 更新架构描述
- **踩坑记录** — 追加到 8.10 节

**不做的事**：不为了更新而更新。没有新发现就跳过这步。

### 轻量 vs 重量：按规模选择

不是每次改代码都要跑完 8 步。规模决定深度：

| 改动规模 | 跑哪些步骤 | 举例 |
|----------|-----------|------|
| **微调**（< 10 行） | ④ 直接改 | 改个标签文字、调个颜色 |
| **小功能**（10~100 行） | ①④⑤ | 加个按钮、加个检查规则 |
| **中功能**（100~500 行） | ①②④⑤⑥ | 新增一个检查器面板 |
| **大功能 / 新工具**（500+ 行） | 全部 8 步 | 新建 EditorWindow 工具 |

AI 在收到需求后，先判断规模，告诉用户"这个大概是中功能级别，我会跑 ①②④⑤⑥"。

---

## 一、代码重构铁律

> **2026-03-27 血的教训**：用 PowerShell 正则脚本自动替换 GUIStyle 变量名 `s`，
> `\b` 匹配短变量名导致正则表达式、字符串字面量、函数参数被全局污染，
> 7+ 个文件严重损坏，被迫 P4 全量回滚。

### 六条铁律（不可违反）

1. **绝不用正则做 C# 批量重构** — `\b` 匹配短变量名会污染正则表达式、字符串字面量、函数参数。只用手工逐文件替换或 AST 感知工具。
2. **改代码前先确认可回滚** — P4 项目没有 git，改之前必须告诉用户"我要改这些文件"，让用户确认有回滚手段。
3. **改完一个文件立即验证** — 不要攒一堆改动最后才发现炸了。做一个验一个。
4. **出了问题不要硬修** — 超过 2 轮修不好就停下来，告诉用户需要回滚哪些文件，让用户操作。不要越修越烂。
5. **linter ≠ 编译器** — VSCode/OmniSharp 说零错误不等于 Unity 能编译通过。不要以 linter 结果作为"修好了"的依据。
6. **不写 PowerShell/Bash 脚本做代码变换** — 这不是文本替换，是代码变换，需要理解语法树。

### 重构前备份检查单（每次重构开始前必须执行）

开始任何涉及多文件修改的重构前，按以下顺序执行：

1. 列出即将修改的所有文件清单
2. 提醒用户："这些文件即将被修改，请确认已有备份/回滚手段（P4 shelve、.bak 文件、或其他方式）"
3. 等用户明确确认后再动手
4. 如果是 P4 项目，提醒用户先 Checkout 相关文件

未经用户确认，**禁止**开始修改文件。

### 安全重构四件套（修老代码时必须选用）

> **背景**：修老代码比写新代码危险十倍。业界三大流派（Feathers 特征测试、Fowler 绞杀者模式、2025-2026 AI 辅助重构）
> 的核心共识是：**不是靠"小心点"来避免出错，而是靠机制让出错时能被立刻发现并回滚。**

根据改动规模，从以下四个工具中选择组合使用：

#### 工具一：不变量声明（所有改动必须执行）

开始任何重构前，AI 必须先输出不变量声明：

```
## 不变量声明
- 要改的：[具体说明改什么、怎么改]
- 不能变的：
  - 界面外观（按钮位置、颜色、文字不变）
  - 功能逻辑（输入输出行为不变）
  - 公共接口（方法签名、参数类型不变）
  - [其他具体约束]
```

用户看过声明后，改完可以针对性验证"不能变的"有没有变。
**没有不变量声明就开始改代码 = 违规。**

#### 工具二：改前快照（中等以上改动执行）

> 来源：Michael Feathers "特征测试 / 黄金主控" 的简化版，适配无测试框架环境。

在改代码之前：
1. **操作快照**：在 Unity 里把当前功能跑一遍，请用户记录关键输出（截图 / Console 日志 / 结果数量）
2. **代码快照**：把原文件复制一份 `.bak`（P4 项目用 shelve）

改完之后，同样操作一遍，跟快照对比：
- 输出跟改前一样 = 没改坏
- 输出不一样 = 要么是有意改的（bug 修复），要么是无意改坏的

> 关键：**快照记录"现在是什么"，不管对不对。先锁定行为，再谈改善。**

#### 工具三：绞杀者模式（God Class / 大规模重构时使用）

> 来源：Martin Fowler 2004 年提出，2025-2026 仍是业界改造遗留系统的主流策略。

**不在老代码里动刀，在旁边写新的替代它。** 流程：

1. **新建替代文件**：在旁边写 `XXX_V2.cs`（或更好的命名），实现同样的功能
2. **逐个迁移调用**：老代码里的调用逐个改成调用新文件
3. **并行验证**：新旧代码并存期间，两边都能跑，随时可以切回旧代码
4. **删除旧代码**：所有调用都迁移完毕、验证通过后，删除旧文件

好处：
- 旧代码始终完整可回滚（因为你没改它）
- 新代码从一开始就是干净的架构
- 如果新代码出问题，把调用改回旧的就行

**适用场景**：P4SubmitChecker（6874 行）、SceneAssetChecker（5995 行）等 God Class 的拆分。

#### 工具四：单点手术（小 bug 修复 / 小改动时使用）

**只改一个方法，不顺手改旁边的。** 严格遵守：

1. 一次只改一个方法体内的代码
2. 不改方法签名（除非这就是本次要改的东西）
3. 改完立刻编译 + 测试这一个功能
4. 确认没问题后再改下一个
5. **每改 3~5 个方法就请用户在 Unity 里编译验证一次**

> "顺手改"是万恶之源。你去修一个 GUIStyle 缓存，顺手改了旁边的变量名，
> 结果变量名改错了，但你以为是 GUIStyle 缓存导致的 bug，越修越远。

### 按改动规模选择组合

| 改动规模 | 必选 | 推荐 |
|----------|------|------|
| 修一个 bug / 改一行 | 不变量声明 + 单点手术 | — |
| 缓存 GUIStyle / 改 5~10 处 | 不变量声明 + 单点手术 | 改前快照 |
| 提取方法 / 拆分文件 | 不变量声明 + 改前快照 | 绞杀者模式 |
| God Class 拆分 / 大规模重构 | 不变量声明 + 改前快照 + 绞杀者模式 | — |

### 老代码消化策略（系统性变好，不是靠"顺手"）

> **核心问题**：台账记了问题，安全重构四件套防翻车，但"什么时候修、按什么顺序、每次修多少"没有定义。
> 没有策略的"顺手消化"= 失控的连锁修改 = 又翻车。

#### 两条路径：被动消化 vs 主动消化

| 路径 | 触发条件 | 做什么 | 安全边界 |
|------|----------|--------|----------|
| **被动消化** | 因新功能/bug 修复**必须**碰某个文件时 | 顺手修掉该文件里的台账问题 | 本次顺手修的量 ≤ 本次功能改动量 |
| **主动消化** | 用户专门拿出时间说"今天消化技术债" | 按优先级框架选择目标，专门修 | 一次只修一个台账条目 |

#### 被动消化的安全边界（"顺手"的尺度）

**铁律：顺手改的量不能超过正事改的量。**

举例：
- 你改 `SceneAssetChecker.cs` 加一个新检查规则（正事，改了 30 行）→ 可以顺手缓存该文件里 2~3 个 GUIStyle（顺手，改 10 行）
- ❌ 你改一行 bug fix → 顺手重构 200 行 → **这不叫顺手，这叫借题发挥，禁止**

具体规则：
1. **只修同文件**——不要因为顺手而跨文件改
2. **只修台账里已记录的问题**——不要修着修着发现新问题也一起改
3. **顺手改完也要过不变量声明**——确认没影响到不相关的功能
4. **超出安全边界时 AI 必须刹车**：告诉用户"这个顺手修的量太大了，建议单独开一轮主动消化"

#### 主动消化的节奏

用户说"今天消化技术债"时：
1. AI 读台账，按优先级框架（见下文）推荐 1~3 个条目
2. 用户选一个
3. 走安全重构四件套（不变量声明 → 改前快照 → 改 → 验证）
4. 改完更新台账状态（⬜ → ✅）
5. **一个条目完全搞定后**，再问用户"要继续下一个吗？"

> 关键：**一次只开一个手术台**。不要同时修两个台账条目。

### 技术债优先级框架（Debt Triage）

> **用途**：台账里几十个问题，不可能全修，也不应该全修。这个框架决定"先修什么、后修什么、永远不修什么"。

#### 四级优先级

| 级别 | 标准 | 什么时候修 | 台账里的典型例子 |
|------|------|-----------|----------------|
| **P0 — 定时炸弹** | 会导致崩溃、数据丢失、死锁、安全漏洞 | **尽快修**，下次碰到这个文件必须修 | SYS-01 Process 死锁（缓冲区满永久阻塞） |
| **P1 — 慢性毒药** | 不会崩溃但持续制造痛苦：重复代码导致改一处漏一处、God Class 导致每次改都可能误伤 | **主动消化时优先选**，被动消化时如果顺手量合适也修 | SYS-04 三套重复 P4 实现、SYS-03 God Class |
| **P2 — 性能债** | 不影响正确性，但影响体验：卡顿、GC 抖动、每帧分配 | **被动消化时顺手修**，不值得专门花时间 | SYS-02 OnGUI new GUIStyle 62 处 |
| **P3 — 美容债** | 命名不规范、注释缺失、格式不统一 | **永远不专门修**，碰到了才顺手改 | 变量命名不符合 7.3、缺注释 |

#### 优先级判定规则

AI 在台账里给新问题定级时，按以下顺序判断：
1. **会不会崩？** 会 → P0
2. **会不会导致连锁修改失败？**（改一处必须同步改 N 处，漏了就出 bug）会 → P1
3. **用户能感知到性能问题吗？** 能 → P2
4. **以上都不是？** → P3

#### 什么时候算"修完了"

台账条目从 ⬜ 改为 ✅ 的标准：
- 代码已改 + 编译通过 + 用户在 Unity 里验证功能正常
- 如果是 P0/P1：必须走完改前快照 + 改后对比
- 如果是 P2/P3：编译通过即可

### God Class 拆分操作手册（绞杀者模式实操）

> **适用对象**：单文件超过 3000 行的巨型类（SceneCraft 的 P4SubmitChecker 6874 行、SceneAssetChecker 5995 行等）。
> **核心理念**：不在 God Class 里动刀，在旁边长出新代码，逐步替代。

#### Step 0：评估是否值得拆

不是所有 God Class 都需要拆。问三个问题：
1. **这个类还在频繁新增功能吗？** 不再新增 → 不拆，留着用
2. **每次改它都容易出 bug 吗？** 不容易 → 不拆
3. **多人（含 AI）需要同时修它吗？** 不需要 → 不拆

> 三个都是"否" → **不拆**。God Class 不好看，但"能用且不常改"就别碰它。

#### Step 1：识别可独立的"器官"

读代码（或读架构速查文档），找出 God Class 里**职责独立的方法群**：

```
例：P4SubmitChecker 6874 行
├── P4 连接/登录逻辑（~800 行）→ 可独立为 P4ConnectionManager.cs
├── 文件差异分析逻辑（~1200 行）→ 可独立为 P4DiffAnalyzer.cs
├── 提交检查规则（~2000 行）→ 可独立为 P4SubmitRules.cs
├── GUI 绘制（~2500 行）→ 留在原类（它是 EditorWindow）
└── 工具方法（~300 行）→ 归入 P4AssetHelper.cs
```

**判断标准**：这组方法只访问自己的数据，不跟其他方法群共享字段 → 可独立。

#### Step 2：一次只摘一个"器官"

**绝不同时拆两个。** 流程：

1. 选一个最独立的方法群（依赖最少的）
2. 新建文件（`P4ConnectionManager.cs`），把方法原封不动复制过去
3. 新文件里调通编译（可能需要调整访问级别、传参）
4. **旧文件里这些方法先不删**——改成调用新文件的对应方法：
   ```csharp
   // 旧：P4SubmitChecker.cs 里的方法
   private bool P4Login(string password)
   {
       // 以前 200 行的实现...
   }

   // 改成转发：
   private bool P4Login(string password)
   {
       return _connectionManager.Login(password);  // 转发到新类
   }
   ```
5. 编译 + 用户验证功能不变
6. 确认 OK 后，**下一轮**再考虑删掉旧方法里的转发，让调用方直接用新类

#### Step 3：验证"器官移植"成功

每摘一个器官后：
- [ ] 新文件编译通过
- [ ] 旧文件编译通过（转发到新文件）
- [ ] 用户在 Unity 里操作相关功能，行为不变
- [ ] 台账更新：标记 God Class 拆分进度

#### Step 4：重复直到 God Class 瘦到合理体量

目标不是"完全消灭 God Class"，而是瘦到 **< 1500 行**。
- EditorWindow 类保留 GUI 绘制逻辑（这本来就该在这里）
- 业务逻辑、数据处理、外部进程调用全部在独立文件里

#### 拆分安全红线

- ❌ **不改公共接口签名**——其他文件调用的方法，签名保持不变，内部改成转发
- ❌ **不改已有功能的行为**——拆分是结构变化，不是逻辑变化
- ❌ **不在拆分的同时修 bug**——一次只做一件事
- ❌ **不一次提交所有拆分**——每摘一个器官，编译验证一次，确认 OK 后再下一个

---

## 二、Unity 编辑器工具开发偏好

### UI/UX 原则

| 原则 | 说明 |
|------|------|
| **文字优于图标** | UI 中使用文字标签，不用 emoji/图标。用户明确要求 emoji 时除外 |
| **颜色要可区分** | 相邻状态的颜色必须有明显区分度，避免灰色和浅色混淆。需要特别注意：需关 Unity 的项目用紫色 `(0.7, 0.5, 1.0)` 而非灰色 |
| **不在 OnGUI 执行阻塞操作** | 绝不在 `OnGUI()` / `OnInspectorGUI()` 中自动执行外部进程、网络请求或耗时命令。这类操作必须由用户点击按钮手动触发 |
| **输入框可编辑+预设下拉** | 配置项（如服务器地址、路径）提供可编辑输入框 + ▼ 下拉预设菜单，兼顾灵活性和便利性 |
| **错误重试不限次数** | 密码输入、连接重试等场景，错误后继续弹框让用户重试，只有用户点「取消」才退出。不要自动放弃 |
| **颜色图例固定可见** | 列表上方的颜色图例条使用 ■ 方块 + 实际颜色 + 文字说明，固定不随滚动消失 |
| **子节点继承父节点样式** | 树形结构中，子节点应继承父节点的颜色/样式标记（如 `parentIsArt` 参数传递） |
| **耗时操作加提示** | 点击后需要等待的按钮，旁边加"点击后请耐心等待"文字提示 |
| **首次操作加说明** | 首次使用的检测/连接按钮，附加详细说明文字，降低用户困惑 |

### 代码组织原则

- 初始状态使用特殊值（如 `-2` 表示"从未检测"），与正常状态值 `0`/`1` 区分
- 环境变量检测结果要缓存（如 `_p4EnvScanned`），避免每帧重复执行
- EditorPrefs 保存用户配置时，PREFS_PREFIX 保持旧前缀以向后兼容
- 工具自身的文件在列表中自动过滤（如过滤 `/SceneCraft/` 路径）
- 路径探测采用多级回退：缓存 → EditorPrefs → 内嵌 → PATH → 常见路径

### 单文件工具的务实态度

对于 Editor 工具类，如果功能内聚且团队只有少数人维护，**单文件大类是可接受的**（如 5000+ 行的 EditorWindow）。不要为了"架构正确"强行拆分导致维护成本增加。但要保持良好的 `#region` 分区和注释。

### 编辑器 UI 技术选型（IMGUI vs UI Toolkit）

| 场景 | 推荐 | 理由 |
|------|------|------|
| **维护现有工具**（SceneCraft 等） | IMGUI | 15000+ 行 IMGUI 代码，迁移成本远大于收益。IMGUI 不会被废弃 |
| **从零开始的新工具** | **优先评估 UI Toolkit** | 数据绑定、UXML/USS 分离布局与逻辑、热重载样式，长期维护更省力 |
| **快速原型 / 一次性小工具** | IMGUI | 上手快，不需要建 UXML/USS 文件，几十行搞定 |

> **不强制迁移**。这是建议而非铁律。选哪个取决于工具的预期寿命和复杂度。
> 如果新工具预期会长期维护且 UI 复杂（多页签、列表、拖拽），UI Toolkit 的投资会回本。
> 如果只是写个小脚本弹个窗口，IMGUI 依然是最快的路。

### UI 排版布局规则

| 规则 | 说明 |
|------|------|
| **Toolbar 固定顶部** | 工具栏（按钮行、颜色图例条）固定在窗口/区域顶部，不随 ScrollView 滚动消失 |
| **功能区用 HelpBox 包裹** | 逻辑上独立的功能块用 `EditorStyles.helpBox` 背景包裹，视觉分隔清晰 |
| **Foldout 分组折叠** | 多个功能模块使用 Foldout 折叠分组，默认展开核心功能、折叠次要功能 |
| **列表区用 ScrollView** | 文件列表、检测结果等可变长内容必须包在 `EditorGUILayout.BeginScrollView` 中 |
| **状态信息内联显示** | 连接状态、用户名等关键信息直接显示在相关控件旁（如 "✅ P4 已连接 (用户名@workspace名)"），不要单独弹窗 |
| **页签分离大功能** | 功能差异大的模块用页签（Tab）分离（如"提交预检" vs "版本管理"），不要挤在一个长页面 |
| **输入框+下拉预设并排** | 可编辑输入框右侧紧贴 ▼ 下拉按钮，点击展开预设列表（如服务器地址预设） |

### 颜色编码系统

预览列表等场景中的颜色图例使用 **■ 方块 + 实际颜色 + 文字说明** 的格式：

| 颜色 | 用途 | 备注 |
|------|------|------|
| **美术场景色**（自定义） | `scene_making/scenes` 下的美术场景文件 | ART_FOCUS_FOLDERS 只限核心场景目录 |
| **绿色** | 其他美术资源（非场景的美术目录文件） | ART_FOCUS_FOLDERS 缩小后，其余美术用绿色 |
| **冲突色**（自定义） | 有冲突的文件 | |
| **上次跳过色**（自定义） | 上次同步时被跳过的文件 | |
| **紫色** `(0.7, 0.5, 1.0)` | 需关 Unity 才能同步的文件/文件夹 | ❗ 不用灰色，避免和普通灰色混淆 |
| **非美术新增色**（自定义） | 非美术目录的新增文件 | |
| **黄色**（文字色） | 非场景目录含场景子文件时的提示文字 | 如 "含N个场景更新" |
| **绿色**（高亮背景） | Workspace 选择中自动匹配的 workspace | 绿色高亮 + 一键按钮 |
| **灰色** | Workspace 选择中不匹配的 workspace | 灰色显示但允许强制使用 |

**颜色调整历史教训**：
- 需关 Unity 的项最初用灰色，和普通灰色混淆 → 改为紫色
- 图例最初用纯文字描述 → 改为 ■ 方块 + 实际颜色，直观

### 分步引导 UI（Step Wizard）模式

适用于需要多步骤初始化/连接的场景（如 P4 连接引导）：

1. **每步独立状态** — 每个 Step 有自己的检测状态变量（如 `_p4ExeFound`、`_p4EnvScanned`、`_p4LoginState`）
2. **初始状态用特殊值** — 用 `-2` 表示"从未检测"，区别于 `0`（检测失败）和 `1`（检测成功），避免 OnGUI 误触发
3. **步骤间有依赖** — Step 2 依赖 Step 1 通过，Step 3 依赖 Step 2 通过，未通过的步骤不展示后续内容
4. **每步有独立按钮+结果显示** — 检测按钮 + 检测结果（成功/失败 + 详细信息）+ 修复操作按钮
5. **检测结果要缓存** — 如 `_p4EnvScanned` 防止每帧重复执行耗时操作
6. **重置时全链路同步** — 重置某步状态时，必须同步重置其依赖的缓存标记（如 `_p4LoginState = 0` 时同步重置 `_p4EnvScanned`）

### 列表/树形展示规则

| 规则 | 说明 |
|------|------|
| **颜色图例固定顶部** | 图例条在 ScrollView 外面，不随列表滚动 |
| **子节点继承父节点样式** | 树形结构中子节点继承父节点颜色标记（通过 `parentIsArt` 等参数传递） |
| **工具自身文件自动过滤** | 列表中自动过滤掉工具自己的文件（如过滤 `/SceneCraft/` 路径的条目） |
| **非叶子节点含特定子项加提示** | 非场景目录如果包含场景子文件，加黄色 "含N个场景更新" 提示 |
| **ART_FOCUS_FOLDERS 要精确** | 焦点目录只限核心目录（如 `scene_making/scenes`），不要范围过大 |
| **需关 Unity 的文件夹列表可扩展** | `Settings`、`scripts_dll` 等需关 Unity 同步的目录应维护在可配置列表中 |

### 连接/登录流程设计

**零配置理念**：用户打开工具应该尽可能不需要手动输入任何配置。

| 阶段 | 设计 |
|------|------|
| **探测可执行文件** | 多级回退：缓存 → EditorPrefs → 内嵌文件 → PATH 环境变量 → 同类进程探测 → 常见安装路径 |
| **读取环境配置** | 自动从 `p4 set` 命令输出 + Windows 注册表读取配置值，缺失时自动补全 |
| **默认值填充** | 无配置时，服务器地址自动填公司主服务器（如 `192.168.31.15:1667`），不留空 |
| **输入框可编辑** | 自动探测到的值显示在**可编辑**输入框中，允许用户手动修改 |
| **密码登录** | Unity 内直接弹密码输入框，通过 `Process.StandardInput.WriteLine(password)` 喂给 `p4 login`，不需要开命令行 |
| **回车确认** | 密码输入框支持回车键直接提交 |
| **ticket 过期** | 自动弹 `EditorInputDialog` 密码框，支持回车确认，至少 3 次重试机会 |
| **完全零配置** | 提供服务器地址 + 用户名 + 密码三合一界面，一键"配置并登录" |

### 按钮交互模式

| 模式 | 说明 | 示例 |
|------|------|------|
| **一键排除** | 一个按钮同时排除多种危险/不可操作项 | 「仅安全」同时排除冲突文件 + 需关 Unity 文件夹 |
| **内联下拉** | 状态行旁附带下拉按钮，不跳转页面 | 已连接状态行的「切换分支」下拉、P4PORT 的 ▼ 服务器预设 |
| **状态行内联信息** | 连接成功后在同一行显示关键上下文 | "✅ P4 已连接 (用户名@workspace名)" |
| **首次操作加说明** | 用户从未执行过的操作按钮附加详细说明文字 | 首次检测 P4 连接按钮下方的说明文字 |
| **耗时操作加等待提示** | 执行后需等待的按钮旁标注提示 | 预览更新按钮旁 "点击后请耐心等待" |
| **一键自动修复** | 检测到问题时提供一键修复，不让用户手动执行命令 | Workspace 不匹配 → 扫描 + 一键 ApplyWorkspace |
| **匹配高亮 + 强制可用** | 自动匹配项绿色高亮 + 一键确认；不匹配项灰色但仍可强制使用 | Workspace 选择列表 |

### Workspace 选择 UI

- 显示 **stream 分支名**和**描述信息**，支持多分支开发场景
- 自动匹配的 workspace 绿色高亮 + 一键应用按钮
- 不匹配的灰色显示，但允许强制使用（别拦死用户）
- 状态行加「切换分支」下拉按钮和「刷新」按钮

---

## 三、功能模块间接口设计

### 共享工具类

- 多个编辑器工具共用的功能（如 `GetP4ExePath()`）抽到共享工具类（如 `P4Helper`）
- 共享方法修改时，**所有调用方必须同步更新**（如 `P4AssetHelper` 和 `P4SubmitChecker` 同步更新 `GetP4ExePath()`）
- 共享类以 `static` 方法或单例提供，不要每个工具各自实现一份

### 模块间通信

- 模块间**不直接引用彼此的实例**，通过以下方式解耦：
  - **EditorPrefs**：用统一的 `PREFS_PREFIX` 存取配置（如 `"PrefabMaster_P4Port"`）
  - **共享工具类**：公共方法作为桥接
  - **事件/回调**：需要实时通知时用 `System.Action` 回调，不用轮询
- EditorPrefs key 命名规范：`{PREFS_PREFIX}{模块名}_{配置项}`

### 向后兼容

- `PREFS_PREFIX` 和 `TRACEABILITY_PREFIX` 保持旧前缀（如 `"PrefabMaster_"`），即使工具已改名
- 改名时不迁移旧配置，直接兼容读取

---

## 四、Git 版本管理规范

### Commit 消息格式

```
[office/home] 类型: 简短描述
```

- **公司电脑**（leijiang / hermanlei@tencent.com）：`[office]` 前缀
- **家里电脑**（lj860117）：`[home]` 前缀
- **类型**：`feat` / `fix` / `perf` / `style` / `refactor` / `docs`

示例：
```
[office] feat: 添加 P4 workspace 自动选择功能
[home] fix: 修复 OnGUI 每帧执行 P4 命令导致卡死
```

### 版本管理

- 阶段性完成时用 `git tag` 打语义化版本号（如 `v1.3.0`）
- 打 tag 前更新项目根目录的 `CHANGELOG.md`
- PowerShell 中 commit 含特殊字符时，用 `-F` 文件方式传递消息（文件需 UTF-8 编码，用 write_to_file 而非 echo）

---

## 五、P4 (Perforce) 工作流

### 操作规范

- P4 项目**没有 git 的撤销能力**，所有修改前必须确认回滚手段
- 批量修改前提醒用户先 `p4 edit`（Checkout）相关文件
- 使用 `p4 -s` (tagged output) 解析命令输出，比纯文本解析更可靠
- P4CLIENT 环境变量经常是错误的（机器名默认值），工具中需要主动检测和修复
- 工具内嵌 `p4.exe` 时，目录名不能用 `~` 后缀（如 `Bin~`），Unity 不生成 .meta 会导致 P4 trigger 报错

### Unity + P4 集成注意

- P4V 的 workspace 配置和 `p4` 命令行用的 P4CLIENT 可能不一致
- 通过 `p4 clients -u 用户名` 列出 workspace，按 Root 路径匹配工程
- 密码登录通过 `Process.StandardInput.WriteLine(password)` 喂给 `p4 login`

---

## 六、项目特定约定

### 项目识别索引（AI 匹配规则）

> 用户提到某个功能/bug 时，AI 根据下表的「别名/口语」列匹配项目。**匹配不到任何一行时必须询问用户**，不得猜测。

| 项目 | 别名 / 口语关键词 | 命名空间 | 文件路径特征 | 技术栈 | 参与模式 | 适用规范章节 |
|------|-------------------|---------|-------------|--------|---------|-------------|
| SceneCraft | 场景工坊、工具、编辑器工具、SceneCraft | `SceneCraftKit` | `SceneCraft\Editor\` | Unity IMGUI 编辑器扩展 | 🔑 主导 | 七 + 八 |
| biosphere-game | 游戏、小游戏、biosphere | — | `biosphere-game\` | HTML/CSS/JS 网页游戏 | 🔑 主导 | 七 |
| hermanlei-conventions | Skill、规范、约定 | — | `.workbuddy\skills\hermanlei-conventions\` | Markdown 文档 | 🔑 主导 | 四 |
| 公司 Unity 项目 | 项目、15hb、unity 项目 | — | `G:\Project_15hb\` | Unity + EcoEngine | 🤝 协作 | 铁律 only |

> **参与模式说明**：
> - 🔑 **主导**：用户定规范、做架构决策 → 全套铁律+规范+闭环流程适用
> - 🤝 **协作**：别人的规范，用户参与开发 → 只遵守铁律（安全底线），不主动推架构偏好
> - SceneCraft 内有 cat1986 维护的子模块（Hierarchy 文件夹、模型导出 & 检查），这些子模块按🤝协作模式对待，详见 scenecraft-architecture.md 第七章
>
> **以后新增项目时，在此表追加一行即可。**

#### 消歧义交互规则

> **当 AI 无法确定用户说的是哪个项目或模块时，必须用选择框（`ask_followup_question`）让用户点选，而非文字追问。**

**第一层：识别项目**（匹配不到时触发）
- 选项从上表动态生成，末尾固定加一个「其他 / 新项目」

**第二层：识别模块**（确定了项目但不确定具体模块时触发）
- 选项从该项目的架构速查文档（如 `references/scenecraft-architecture.md`）中的模块列表生成
- 如果没有速查文档，用简短文字追问即可

**不需要弹选择的情况**：
- 上下文已经很明确（比如正在连续讨论同一个项目的代码）
- 用户的描述能精确匹配到索引表中的某一行

#### Skill 自动路由规则

> AI 根据用户需求的领域关键词，**自动加载**对应 Skill，无需用户手动指定。
> 多个 Skill 匹配时，按优先级加载（最多同时 2 个）。
> 用户也可以直接说"用 XXX Skill"强制指定。
> **路由表中没有覆盖的领域（标记「待补充」的行）**：必须用选择框询问用户"要不要搜索合适的 Skill"。用户选择搜 → 用 `find-skills` 搜索并推荐；用户选择不搜 → 用当前角色知识直接回答。**不得跳过询问自行判断。**

| 需求领域 | 触发关键词 | 推荐 Skill | 备注 |
|----------|-----------|-----------|------|
| Unity 编辑器工具 IMGUI | EditorWindow, Inspector, IMGUI, 编辑器工具, 自定义面板 | `unity-editor-imgui-design` | 可叠加 `unity-workflows` |
| Unity 编辑器自动化 | 自动化, 批量操作, 菜单脚本 | `unity-workflows` | |
| Unity UI 系统选型 | UI 选型, UGUI 还是 Toolkit, 该用哪个 UI | `unity-ui-selector` | 选完后按结果加载对应 Skill |
| Unity UI Toolkit | UI Toolkit, USS, UXML, VisualElement | `unity-uitoolkit` | |
| Unity 代码架构 / SO | ScriptableObject, 解耦, 事件通道, 架构设计 | _(当前会话已有 Unity架构师角色)_ | 角色自带，无需额外 Skill |
| Unity 场景性能优化 | 性能, draw call, 帧率, 卡顿, batching | `unity-scene-optimizer` | |
| Unity 代码质量 | 代码审查, review, 规范检查, 最佳实践 | `unity-script-validator` | |
| Unity 编译错误修复 | 编译报错, CS0xxx, 红色错误, 编译不过 | `unity-compile-fixer` | |
| Unity 新脚本模板 | 新建脚本, 生成模板, MonoBehaviour 模板 | `unity-template-generator` | |
| Unity 测试 | 单元测试, PlayMode, EditMode, Test Runner | `unity-test-runner` | |
| Unity 关卡/场景设计 | 关卡设计, 地图布局, 场景搭建, 原型 | `level-design-patterns` | 可叠加 `unity-level-design` |
| 游戏设计 / 策划 | 玩法, 机制, 数值, 策划, 系统设计 | `game-developer-skill` | |
| Shader / 渲染 | Shader, 材质, 渲染管线, URP, HDRP, GI, 光照 | 先读 `references/rendering-knowledge-index.md` 定位方向，再搜索具体资料 | 有知识索引，可叠加 `shadergraph-editor`(visionOS) |
| 2D / 3D 美术表现 | 美术风格, 动画, 粒子, VFX, 2D 美术 | _(待补充，遇到时用 find-skills 搜索)_ | |
| 后端开发 | 服务器, API, 数据库, 网络, 后端 | _(待补充，遇到时用 find-skills 搜索)_ | |
| Git / GitHub | PR, issue, CI, Actions, GitHub | `github` | |
| 图片生成 | 生成图片, 画一张, 概念图 | `nano-banana-pro` 或 `openai-image-gen` | |

> **维护说明**：安装新 Skill 后，在此表追加对应行。标记「待补充」的行表示该领域尚无专用 Skill，AI 应在用户首次触发时主动搜索推荐。

#### Skill 自我成长规则

> Skill 不只靠用户主动喂养——AI 必须在日常对话中**主动发现**值得沉淀的内容并提议更新。

**主动提议触发条件**（满足任一即触发）：

1. **新最佳实践**：AI 在回答中给出了重要的规范/模式/技巧，但当前 Skill 里没有记录
2. **工作流变更**：发现用户的项目结构、工具链、开发习惯发生了变化（新项目、新路径、新工具）
3. **重复问题**：同一类问题在不同会话中被问到第二次——说明值得写成规则
4. **新反模式/踩坑**：发现了一个新的坑或反模式，值得加入铁律
5. **路由表缺口**：某个领域被频繁触发但没有专用 Skill，说明该补了
6. **规则过时**：发现 Skill 中某条规则已不适用当前项目状态（路径变了、工具换了、流程改了）

**提议格式**：
```
💡 建议更新 Skill：[具体建议内容和理由]
```

**执行规则**：
- 提议放在回答末尾，**不打断当前工作流**
- 用户确认后才执行更新，**不得自动修改 Skill**
- 一次最多提 2 条建议，避免信息过载
- 琐碎/临时性的内容不提议（只沉淀有长期价值的）

#### Skill 成长引擎（三个定期机制）

> Skill 不是一次性写完的文档，是一个**活的知识系统**。以下三个机制确保它持续进化。

##### 机制一：每周巡检（健康检查）— 自动化任务，每周一 14:30

> 维护现有内容的准确性。

**巡检内容**：
1. 路由表中「待补充」的领域，搜索是否有新的合适 Skill 可用
2. 已安装 Skill 列表与路由表是否一致（有没有装了但没加到表里的）
3. Skill 中引用的文件路径是否仍然有效
4. 最近一周的 working memory 日志中，是否有反复出现但未被 Skill 覆盖的模式
5. 是否有规则因项目变化而需要更新
6. **错过补救检查**：检查上次巡检日期（记录在 working memory 的 MEMORY.md 中），如果距今 >10 天说明中间错过了巡检，自动补上
7. **内容退化/过时检测**（🆕）：
   - 检查每个项目/领域在最近 30 天 working memory 中是否被提及。**超过 60 天未提及的领域 → 标记为"休眠"**
   - 休眠领域的 SKILL.md 专属章节和 references/ 文件，在巡检报告中列出并建议用户："XXX 领域已 60 天未活跃，是否需要精简/归档相关内容？"
   - 检查 references/ 中每个文件的"最后更新"日期，**超过 90 天未更新的文档 → 标记为"可能过时"**
   - 巡检分拆信号表（第 4 条）：检查是否有某个领域的专属内容超过总量 40%

**MEMORY.md 中需要维护的活跃度追踪**（AI 每次在某领域做了实质工作后更新）：
```markdown
## 领域活跃度
- scenecraft: 2026-03-30
- biosphere-game: 2026-03-28
- rendering: 2026-03-30
- hermanlei-conventions: 2026-03-30
```
> 格式：`领域名: 最近一次实质工作日期`。巡检时用这个判断是否休眠。

**输出**：简短报告 → 用户确认后执行。

**错过补救机制**：
> 自动化任务依赖 WorkBuddy 运行。如果用户某天没打开 WorkBuddy，任务就不会执行。

| 场景 | 补救方式 |
|------|----------|
| 错过了某周的巡检 | 下次打开时，AI 在首次对话开始前检查 MEMORY.md 中的 `last_weekly_check` 日期，超过 10 天自动触发补巡检 |
| 错过了某月的前沿调研 | 下次打开时，AI 检查 `last_monthly_research` 日期，超过 35 天自动触发补调研 |
| 长期不使用（>60 天） | AI 不要一口气补所有错过的，只做一次"回归巡检"：检查 Skill 中所有路径是否有效 + 做一次前沿调研 |

**MEMORY.md 中需要维护的时间戳**（AI 每次执行巡检/调研后更新）：
```markdown
## Skill 成长引擎状态
- last_weekly_check: 2026-03-30
- last_monthly_research: 2026-03-30
```

##### 机制二：前沿调研（知识扩展）— 多频率、按领域

> 不再是"每月一刀切"，而是**不同领域有不同的调研节奏和信息源**。

**领域调研矩阵**：

| 领域 | 调研频率 | 搜索什么 | 更新什么 | 知识索引文档 |
|------|----------|----------|----------|-------------|
| **Unity 编辑器工具** | 每月 | Unity 博客新公告、编辑器 API 变更、UI Toolkit 进展、新的编辑器扩展模式 | `SKILL.md` 中的工具开发规范 | _(体量小，暂不单独建文档)_ |
| **渲染/Shader** | 每月 | SIGGRAPH/GDC 演讲、NVIDIA/AMD 新技术、Unity 渲染管线更新 | `references/rendering-knowledge-index.md` | ✅ 已有 |
| **游戏设计/开发** | 每月 | GDC 游戏设计分享、独立游戏创新玩法、Unity 游戏开发新功能、同类游戏 benchmark | `references/gamedev-knowledge-index.md` | 🆕 首次调研时创建 |
| **编程范式/工程实践** | 每 2 月 | 新的架构模式、AI 辅助开发新趋势、代码质量工具、C# 语言新特性 | `SKILL.md` 中的编码规范和闭环流程 | _(体量小，暂不单独建文档)_ |

**调研流程（每个领域通用）**：
1. AI 搜索该领域 2~3 个关键词
2. 筛选出**对当前项目有实际价值**的发现（不是所有新东西都值得关注）
3. 输出调研报告，每个发现标注等级：
   - ⭐ **值得采纳**：成本低、收益高、与当前项目直接相关
   - 👀 **值得关注**：有潜力但暂不行动
   - ℹ️ **了解即可**：纯学术/实验阶段或与当前场景无关
4. 用户确认后：
   - ⭐ 级 → 写入对应知识索引或 SKILL.md
   - 如果是新领域且没有知识索引 → 按 `references/README.md` 规范创建新文档

**调研成果的流向规则**（新发现该写到哪里？）：

```
新发现
  ├── 是具体技术知识（如"Unity 6.5 新增了 XX API"）→ 对应领域的知识索引
  ├── 是好的编程实践（如"发现一种更好的单例替代"）→ SKILL.md 编码规范章节
  ├── 是新的工具/Skill（如"发现一个好用的 Unity Skill"）→ 路由表
  ├── 是一个新的坑/反模式（如"Unity 6 的 XX 功能有 bug"）→ 对应项目的已知问题台账
  └── 不确定归哪 → 先写 working memory，下次巡检时决定
```

**评估标准**（决定一个新技术/实践是否值得采纳）：
1. **跟我们的场景相关吗？** 不相关 → 跳过
2. **实施成本 vs 收益？** 成本高于收益 → 只标 👀 不采纳
3. **成熟度够吗？** 纯学术/实验 → 标 ℹ️，等它成熟再评估
4. **会不会影响现有架构？** 影响太大 → 谨慎，除非收益巨大

##### 机制三（附加）：成长回顾（学了有没有用上）— 随月度调研一起执行

> 光学不用等于没学。每次月度调研时，**先回顾上个月学到的东西落地了没**。

**回顾流程**（5 分钟，调研之前先做）：
1. **翻上次调研报告**中标 ⭐ 的采纳项 → 逐条检查：实际用上了吗？
2. 分三种结果：
   - ✅ **已落地** — 在哪用的？效果怎样？→ 写一句话总结到调研报告的"回顾"段
   - ⏳ **没机会用** — 还值得保留吗？→ 保留到下个月再看，连续 2 个月没用 → 降级为 👀
   - ❌ **试了不行** — 为什么不行？→ 记录原因，避免下次又采纳同类东西
3. **输出一行总结**："上月 N 个采纳项，M 个落地，K 个待验证，J 个淘汰"

**记录位置**：写在当月调研报告的开头，格式：
```
## 上月回顾（YYYY-MM）
- ✅ XXX — 在 YYY 中使用，效果良好
- ⏳ XXX — 暂未有使用场景，保留观察
- ❌ XXX — 原因：...
> 总结：上月 3 个采纳项，2 个落地，1 个待验证
```

**竞品/同类游戏调研**（游戏领域特有）：
> 当用户在做游戏项目时（当前：biosphere-game），调研不只看技术，也看**同类游戏的设计和表现**：
- 同类游戏的创新玩法和机制
- 同平台（微信小游戏 / 移动端）的技术限制和最佳实践
- 用户体验和界面设计趋势

##### 机制三：沟通模式沉淀（经验提炼）— 每次对话结束时

> 把用户的思维方式和决策模式沉淀下来。

**用户的沟通思路模式**（基于历史对话总结）：

| 模式 | 表现 | AI 应该做的 |
|------|------|-------------|
| **由点到面** | 从一个具体问题出发，拓展到"全球有没有更好的做法" | 不止解决眼前问题，同时做横向调研对比 |
| **先防守后进攻** | 先确保不翻车（安全重构），再追求变好（消化策略） | 回答时先给防护方案，再给优化方案 |
| **实用主义** | "好不好"不看理论多漂亮，看能不能落地、成本高不高 | 每个建议都标注实施成本和收益 |
| **体系化思维** | 不满足于零散的规则，要把规则组织成可检索的体系 | 新内容加入时考虑它在 Skill 中的位置，不要随便塞 |
| **成长导向** | 不只是"现在能用"，更关心"以后怎么越来越好" | 每次重大改动后回头检查 Skill 是否需要更新 |
| **元思维** | 不只关心"做什么"，更关心"怎么让做事的方式本身也在进步" | Skill 自身也需要规范和迭代，不只是内容在长，结构也要优化 |

> AI 在每次对话结束时自问：**这次对话中有没有新的决策模式或偏好值得沉淀？** 有 → 更新此表 + working memory。

##### 机制四：Skill 自身的版本迭代规范 🆕

> Skill 本身也是"代码"，也需要规范来管理它的成长。

**Skill 修改的分级制度**：

| 级别 | 什么改动 | 需要用户确认吗 | 需要 Git 同步吗 |
|------|----------|---------------|----------------|
| **S 级** | 修改/删除 🔴 铁律 | ✅ 必须，且要说明原因 | ✅ 立即同步 |
| **A 级** | 新增/修改 🟡 规范、修改闭环流程 | ✅ 必须 | ✅ 本次对话结束时同步 |
| **B 级** | 更新 🟢 知识索引、新增 references 文档 | ⚠️ 告知即可（用户可否决） | ✅ 积累 2~3 个 B 级改动后统一同步 |
| **C 级** | 更新 ⚪ 路径/约定、修复 typo | ❌ 不需要 | ✅ 下次有其他改动时顺带同步 |

**Skill 结构冻结规则**：
- SKILL.md 的**章节编号和顺序**（零~九）不轻易变动——换模型/换平台时 AI 按编号查找，编号乱了就找不到
- 新增内容优先**在现有章节内追加小节**，不要轻易加新章节
- 确实需要新章节时，编号追加在最后（如「十、XXX」），不要插入中间
- references/ 目录下的文件命名规范见 `references/README.md`

**Skill 大小控制**：
- SKILL.md 主文件控制在 **1500 行以内**——超过了说明有内容该迁移到 references/
- 单个 references 文档控制在 **300 行以内**——超过了说明该拆分
- 迁移判断：如果一段内容是"AI 需要主动遵守的规则" → 留在 SKILL.md；如果是"AI 需要时查阅的知识" → 迁移到 references/

**Skill 分拆触发条件**（满足 2 条以上时建议拆分）：

> 以下条件是给**所有使用此 Skill 的 AI** 看的——不管是什么模型，发现满足条件就要告诉用户"这个 Skill 该拆了"。

| # | 信号 | 量化标准 | 原因 |
|---|------|----------|------|
| 1 | **SKILL.md 行数逼近上限** | > 1300 行（留 200 行安全余量） | 再加内容就要超 1500 行了，先拆 |
| 2 | **references/ 文件总数过多** | > 10 个文件 | 文件多说明领域杂，该按领域归组 |
| 3 | **内容地图出现 3+ 个互不相关的领域** | 内容地图表的"必读"列出现同时需要的章节 < 50% | 说明大部分任务只用 Skill 的一小部分，其余是死重 |
| 4 | **某个项目/领域的专属内容 > 总量的 40%** | SKILL.md 中某章 + 对应 references 总行数 > 全部 40% | 说明该领域该独立为自己的 Skill |
| 5 | **用户活跃方向发生分化** | 同时维护 2+ 个技术栈差异大的项目（如 Unity + Web） | 不同栈的规则互相干扰，拆了各管各的 |
| 6 | **加载时的 token 消耗影响正常工作** | AI 反馈上下文窗口不够 / 频繁截断 | 直接指标，必须减重 |

**拆分方向参考**（到时候怎么拆）：
```
hermanlei-conventions        ← 核心：铁律 + 通用编码规范 + 成长引擎 + 元规范
hermanlei-unity-tools        ← Unity 编辑器工具专属：闭环流程 + 二/三/八章 + SceneCraft references
hermanlei-gamedev            ← 游戏开发专属：游戏架构规范 + biosphere-game 约定
hermanlei-rendering          ← 渲染专属：配合 rendering-knowledge-index.md
```

**拆分时的不变量**（拆了不能丢的）：
- 核心 Skill 保留所有 🔴 铁律 — 铁律是底线，任何子 Skill 都不能覆盖
- 每个子 Skill 的元规范中注明"父 Skill"是 `hermanlei-conventions`
- Skill 自动路由表从核心 Skill 统一管理
- 成长引擎留在核心 Skill，巡检范围扩大到所有子 Skill

---

### SceneCraft（场景工坊）

- **开发路径**：WorkBuddy workspace 中开发
- **同步目标**：`G:\Project_15hb\client\unity\Assets\ArtOnly\SceneCraft\Editor\`
- **命名空间**：`SceneCraftKit`
- **主类**：`SceneCraftWindow`
- **PREFS_PREFIX**：保持旧的 `"PrefabMaster_"` 前缀（向后兼容）

### biosphere-game

- **技术栈**：HTML / CSS / JavaScript 纯前端网页游戏（非 Unity 项目），后端 Supabase
- 使用上述 Git commit 规范（[office]/[home] 前缀）
- 当前最新 tag：`v1.3.0`（2026-03-22）
- **远程仓库**：`https://github.com/lj860117/biosphere-game.git`（origin/main）
- **仓库内容**：游戏代码 + hermanlei-conventions Skill 副本（`.workbuddy/skills/hermanlei-conventions/`）

#### 快捷指令："更新 git"

> 当用户说 **"更新 git"** / **"同步到 git"** / **"把 Skill 更新到 git"** 时，执行以下流程：

1. **同步 Skill 文件**：将 Skill 源目录（见下表）的所有文件（SKILL.md + references/）复制到 biosphere-game 仓库对应位置，覆盖旧文件
2. **git add** `.workbuddy/skills/hermanlei-conventions/`
3. **git diff --stat** 确认改动范围
4. **git commit -F**（UTF-8 临时文件方式），commit 后删除临时文件
5. 如果 `git push` 失败（远程有新提交），先 `git pull --rebase origin main` 再 push
6. push 成功后报告 commit hash 和变更摘要

**路径对照表**（按电脑选择）：

| | Skill 源目录 | biosphere-game 本地仓库 |
|---|---|---|
| **公司电脑** (office) | `C:\Users\hermanlei\.workbuddy\skills\hermanlei-conventions\` | `C:\Users\hermanlei\WorkBuddy\20260317212750\biosphere-game\` |
| **家里电脑** (home) | _(待补充，clone 后填入)_ | _(待补充，clone 后填入)_ |

> **注意**：家里电脑第一次使用前，需要先 `git clone` biosphere-game 仓库，然后把路径填到上表中。

---

## 七、通用 C# 编码规范（所有项目适用）

> **不管是 SceneCraft 编辑器工具、小游戏、渲染模块还是任何 C# 项目，都必须遵守这些规则。**

### 7.1 不要重复自己（DRY）

- **写新功能前，先检查项目中有没有现成方法可以复用**
- 如果两处代码做的事情超过 80% 相似，必须抽取为共享方法
- 共享方法放在**专门的工具类/Helper 类**中，不要散落在各个业务类里

### 7.2 方法和类的体量控制

- **单个方法** ≤ 50 行（超了就拆成子方法，方法名即注释）
- **单个文件** 新写的尽量 ≤ 500 行（历史遗留的大文件容忍，但新文件别再这么大）
- 如果一个方法需要滚动才能看全——它太长了
- 如果一个类能用"和"来描述它的职责——它该拆了

### 7.3 命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| 私有字段 | `_camelCase` | `_playerHealth`, `_syncFileList` |
| 公有属性 | `PascalCase` | `Instance`, `Value`, `MaxHealth` |
| 方法 | `PascalCase` | `Initialize()`, `CalculateDamage()` |
| 常量 | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT`, `DEFAULT_SPEED` |
| 局部变量 | `camelCase` | `filePath`, `hitCount` |
| 布尔变量 | `is/has/can/should` 前缀 | `_isReady`, `hasPermission` |
| 事件回调 | `On` 前缀 | `OnEnable()`, `OnDamageReceived()` |
| 接口 | `I` 前缀 | `IDamageable`, `ISaveable` |

### 7.4 修改现有代码的规矩

1. **改共享类/工具类时，必须检查所有调用方是否需要同步更新**
2. **改方法签名时，全局搜索该方法名**，确认没有遗漏的调用
3. **删除代码前确认没有其他文件引用**
4. **改完一个文件立即验证**——不要攒一堆改动最后才发现炸了

### 7.5 错误处理 & 防御式编程

**外部进程调用：**
- 所有外部进程调用必须设 **超时**（推荐 30 秒），超时后 Kill 进程并给用户明确提示
- 用 `Process.Start()` 时必须重定向 `StandardOutput` + `StandardError`，两者都要读
- 进程退出码非 0 时，把 stderr 内容展示给用户，不要默默吞掉

**文件和路径：**
- 操作文件前先 `File.Exists()` / `Directory.Exists()` 检查
- 路径拼接用 `Path.Combine()`，不要手拼 `"/" + xxx`
- 从用户输入获取的路径要 `Trim()` 并处理末尾多余的 `/` 或 `\`
- 文件读写包在 `try-catch` 里，catch 中给用户人话提示（不是堆栈信息）

**try-catch 原则：**
- **不要大范围 try-catch 吞异常** — 只包裹可能出错的具体操作（IO、进程、网络）
- catch 中必须做两件事：① 给用户看得懂的错误提示 ② `Debug.LogException(e)` 保留完整堆栈
- **绝不** catch 后空处理（`catch { }`）

### 7.6 注释 & 代码文档规范

**必须写注释的地方：**
- 复杂算法 / 业务逻辑前加注释说明**为什么这样做**（不是做了什么——代码本身说明做了什么）
- Hack / Workaround 必须加 `// HACK:` 或 `// WORKAROUND:` 前缀 + 原因说明
- 魔法数字必须有注释或提取为 `const`
- 公共 API 写 `/// <summary>` XML 注释

**注释语言：**
- **用中文**。这是给 hermanlei 和中文团队看的项目
- XML Summary（`/// <summary>`）也用中文，方便 IDE 悬停提示

**TODO / FIXME 标记：**
- `// TODO:` — 计划要做但还没做的
- `// FIXME:` — 已知有问题需要修的
- `// HACK:` — 临时方案，以后要改的
- 所有标记必须附上**简短说明**，不能光写 `// TODO:`

### 7.7 Unity 通用规范（游戏 + 工具都适用）

> 以下规则适用于所有 Unity C# 代码，不限于编辑器工具。

- **`GetComponent` 结果缓存** — 不要在 Update/OnGUI 中每帧调用 `GetComponent<>()`，在 Awake/OnEnable 中缓存
- **字符串比较用 const** — Tag、Layer、Animator 参数等不要用魔法字符串，提取为 `const` 或 `static readonly`
- **`SerializeField` 优于 public** — 需要在 Inspector 暴露的字段用 `[SerializeField] private`，不要直接 public
- **`EditorUtility.SetDirty()` 不能忘** — 通过脚本修改 ScriptableObject/Asset 后必须调，否则改动不保存
- **避免 `Update()` 里做可以事件驱动的逻辑** — 能用事件/回调/协程的，不要每帧轮询
- **`null` 检查注意 Unity 假 null** — Unity 重写了 `==` 运算符，`Destroy` 后的对象 `== null` 为 true 但 `is null` 为 false。统一用 `== null` 而非 `is null`
- **协程异常不会冒泡** — `StartCoroutine` 中的异常只会停止协程，不会抛到外层。关键协程要自己 try-catch

### 7.8 通用踩坑记录

> 所有项目通用的坑。每次踩坑追加到这里。

| # | 踩坑场景 | 后果 | 正确做法 |
|---|---------|------|---------|
| 1 | 用 PowerShell 正则批量替换 C# 代码 | 文件污染，全量回滚 | 手工逐文件替换，绝不用正则脚本 |
| 2 | PowerShell 中 commit message 含特殊字符 | git commit 失败或乱码 | 用 `-F` 文件方式传递，UTF-8 编码 |
| 3 | `EditorUtility.SetDirty()` 忘记调 | SO/Asset 改动重启后丢失 | 修改 Asset 后立即调 `SetDirty` |
| 4 | Unity 假 null（`is null` vs `== null`） | 空引用异常或逻辑错误 | Unity 对象统一用 `== null` |

> **遇到新坑时，必须追加到此表。这是活文档。**

### 7.9 游戏开发特有规范（biosphere-game 等）

> **温度**：🟢 知识级 — 这是初始框架，待实际踩坑后逐步升温为 🟡 规范。
> **详细知识索引**：`references/gamedev-knowledge-index.md`

#### 架构原则（SO 优先 + 组合模式）

1. **共享状态用 ScriptableObject，不用单例/静态字段** — 生命值、分数、配置等运行时共享数据，创建 `FloatVariable`/`IntVariable` 等 SO 资产
2. **跨系统通信用 SO 事件通道，不用直接引用** — `GameEvent : ScriptableObject` + `GameEventListener`，禁止 `GameObject.Find()` / `FindObjectOfType()`
3. **每个 MonoBehaviour 只做一件事** — 能用"和"描述职责的组件必须拆分
4. **预制件必须自包含** — 拖入空场景无报错、无对场景层级的隐式依赖
5. **用 `[SerializeField] private` 暴露字段** — 不要 public 字段满天飞
6. **数据驱动优于硬编码** — 数值、配置、敌人属性等全部走 SO 或配置文件，设计师可在 Inspector 直接调整

#### 性能预算意识

- **Update() 里的逻辑要有理由** — 能事件驱动的不轮询，能协程的不每帧
- **GetComponent 必须缓存** — Awake/OnEnable 中缓存，不在热路径中调用
- **对象池（Object Pool）** — 频繁创建/销毁的对象（子弹、特效、敌人）必须用池
- **移动端帧预算** — 微信小游戏/移动端目标 30fps，每帧预算 ~33ms

#### 游戏特有踩坑记录

| # | 踩坑场景 | 后果 | 正确做法 |
|---|---------|------|---------|
| _(待积累)_ | | | |

> **踩坑后追加到此表 + `references/gamedev-knowledge-index.md`。**

---

## 八、SceneCraft 专属规范（编辑器工具开发）

> **以下规则仅在开发 SceneCraft（场景工坊）及类似 Unity 编辑器工具时适用。**
> 开发小游戏等非工具项目时，遵守第七章通用规范即可，无需看本章。

### ⚠️ 铁律：先读速查文档，禁止遍历源码

> **这是 SceneCraft 开发的第一条规则，优先级最高。**

1. **任何 SceneCraft 相关任务开始前，第一步必须读** `references/scenecraft-architecture.md`（架构速查文档）
2. **通过速查文档定位到具体的 1~3 个文件**，然后只读这几个文件
3. **绝对禁止** 用 `search_file` / `list_dir` 遍历 SceneCraft 全部 28 个 .cs 文件来"了解项目结构"——速查文档就是干这个的
4. **新功能开发时**，先在速查文档中查"快速定位指南"确认代码放哪、改哪，再动手
5. **速查文档过时了怎么办**？如果发现实际文件与文档不符，先完成当前任务，任务结束后更新速查文档

违反此规则 = 浪费用户 token + 浪费时间。没有例外。

### ⚠️ 铁律：改完代码必须同步文档

> **用户经常忘记更新文档，所以这是 AI 的责任，每次改完代码后必须主动执行。**

**触发条件**（满足任意一条就触发）：
- 新增了 .cs 文件
- 删除或重命名了 .cs 文件
- 给现有类新增了重要的公共方法或字段
- 修改了类的职责范围（比如把一个功能从 A 文件搬到 B 文件）
- 新增了共享类 / 工具类
- 新增了配置项、枚举、数据类型

**必须做的事**：
1. **更新架构速查文档** `references/scenecraft-architecture.md` — 加入新文件的类声明、核心方法、依赖关系；或更新已有文件的描述
2. **如果涉及新的共享类或代码组织规则变化**，更新本 Skill 文档中的相关章节（8.1 共享类清单、8.2 新代码放哪里等）
3. **主动问用户**："这次改动要不要更新到架构速查文档里？" — 即使你觉得不需要，也问一句，因为用户自己可能记不住

**时机**：在当前任务的代码改动全部完成、验证通过后，作为收尾步骤执行。不要等到下次任务才想起来。

### 8.1 共享类清单（新功能优先查这些）

| 共享类 | 职责 | 什么时候用 |
|--------|------|-----------|
| `P4AssetHelper.cs` | 所有 P4 操作（edit/add/delete/rename/revert） | 任何 P4 相关功能 |
| `SceneCraftStyles.cs` | 所有 UI 颜色和 GUIStyle | 任何 UI 绘制 |
| `SceneCraftData.cs` | 所有数据类型（枚举/结构体/常量） | 新增数据类型 |
| `SceneCraftSettings.cs` | 所有配置项读写（ScriptableObject） | 新增配置项 |

### 8.2 新增代码放哪里

| 场景 | 放哪 | 示例 |
|------|------|------|
| 主窗口的新页签/新功能 | 新建 `SceneCraftWindow.XXX.cs`（partial class） | 加个"资源统计"功能 → `SceneCraftWindow.Stats.cs` |
| 独立弹窗工具 | 新建独立 `XXXWindow.cs` 文件 | 新的检查工具 → `TextureChecker.cs` |
| 新的数据类型/枚举 | 加到 `SceneCraftData.cs` | 新增一个状态枚举 |
| 新的 UI 样式/颜色 | 加到 `SceneCraftStyles.cs` | 新增一种高亮色 |
| 新的配置项 | 加到 `SceneCraftSettings.cs` | 新增一个路径配置 |
| 多个工具共用的 P4 方法 | 加到 `P4AssetHelper.cs` | 新增 `P4Shelve()` |
| 只有一个工具用的辅助方法 | 放在该工具自己的文件里 | `P4SubmitChecker` 内部的辅助方法 |

### 8.3 SceneCraft 体量限制（补充通用 7.2）

- **单个 partial 文件** ≤ 2000 行（超了考虑拆分新 partial）
- **独立工具文件** ≤ 3000 行为宜（已有的大文件是历史遗留，新文件别再这么大）

### 8.4 编辑器工具特有的状态管理

- **初始状态用特殊值** — `-2` 表示"从未检测"，`0` 表示失败，`1` 表示成功。不要用 `false` 表示"还没检测过"
- **缓存标记防重复执行** — 耗时操作（P4 命令、文件扫描）必须有 `_xxxScanned` 标记，防止 OnGUI 每帧触发
- **重置状态要全链路** — 重置某步骤时，同步重置所有依赖它的下游状态
- **不在 OnGUI 中执行阻塞操作** — 外部进程、文件扫描、网络请求一律由按钮点击触发

### 8.5 编辑器 UI 错误展示

- 非致命错误用 `EditorUtility.DisplayDialog()` 弹窗，标题写清什么操作失败了
- 致命错误（初始化失败）在窗口区域用红色大字显示，不要只打 Console.log
- 错误重试不限次数——只有用户点「取消」才退出

### 8.6 #region 组织规则

- 每个 partial 文件按功能用 region 分区，region 名用中文
- region 嵌套不超过 2 层
- 同类方法放同一个 region（所有 Draw 方法放一起，所有数据处理方法放一起）
- **新增 #region 要与现有 region 风格一致**——看同文件中其他 region 怎么命名的

### 8.7 修改 SceneCraft 代码的额外检查

在通用 7.4 的基础上，额外注意：
1. **第一步永远是读速查文档**（见本章开头铁律），通过文档定位文件，不要遍历源码
2. **改共享类（P4AssetHelper / SceneCraftStyles / SceneCraftData / SceneCraftSettings）时，必须检查所有调用方**
3. **partial class 之间的字段引用**——删除代码前确认没有其他 partial 文件在用这个字段

### 8.8 新增 SceneCraft 功能的 Checklist

每次实现新功能前，过一遍这个清单：

- [ ] **第一步：读了架构速查文档**（本章铁律），通过文档定位到具体文件
- [ ] 查了共享类（8.1 表格），确认没有可复用的现成方法
- [ ] 新增代码放对了文件（见 8.2 表格）
- [ ] 新增配置项走了 `SceneCraftSettings.cs`
- [ ] 新增颜色/样式走了 `SceneCraftStyles.cs`
- [ ] 新增数据类型走了 `SceneCraftData.cs`
- [ ] 方法没超 50 行（通用 7.2）
- [ ] 命名符合通用 7.3 规范

### 8.9 代码预审工作流

> **目的**：在专业程序员 Code Review 前主动发现问题，同时避免每次都全量审计浪费 token。

#### 三层预审机制

| 层次 | 名称 | 触发时机 | 范围 | Token 消耗 |
|------|------|----------|------|-----------|
| L1 | **日常守门** | 每次 AI 改完代码提交前 | 仅本次修改的文件 | 极低（已在上下文中） |
| L2 | **模块预审** | 用户要对某模块做大改之前 | 目标模块 1~3 个文件 | 中等 |
| L3 | **全量预审** | 里程碑节点 / 即将被 Code Review | 全代码库 | 高 |

#### L1 日常守门（每次改代码自动执行）

AI 改完代码后，在提交前自动检查本次修改是否触犯以下规则：
1. 是否引入了新的 `new GUIStyle()` 在 OnGUI 路径中？
2. 方法是否超过 50 行？文件是否超过体量限制？
3. 是否使用了同步 `ReadToEnd()` 模式？
4. 是否引入了 `GameObject.Find` / `FindObjectOfType` / 静态单例？
5. 是否有空 catch？是否有硬编码 magic number？
6. 命名是否符合 7.3 规范？

**不需要额外读文件**，因为修改的代码已在上下文中，零 token 额外开销。
**发现问题就地修**，不要等用户指出。

#### L2 模块预审（大改之前执行）

用户说"我要重构 XXX"或"给 XXX 加个大功能"时：
1. 先读已知问题台账 `references/scenecraft-known-issues.md` 中该文件的相关条目
2. 告诉用户"这个文件有 N 个已知问题，要不要顺便修？"
3. 用户确认后，把要修的已知问题和新功能一起做
4. 完成后更新台账（标 ✅）

**核心省 token 策略**：不需要重新读源码找问题，台账里已经有了。

#### L3 全量预审（里程碑时手动触发）

用户说"做一次全面预审"或"马上要被 Review 了"时：
1. 读台账，看还有哪些 ⬜ 未修
2. **只对新增的、台账中没有的文件**做全量逐行审计
3. 已有台账记录的文件，只需抽查台账中标 ✅ 的问题是否真的修好了
4. 更新台账

#### 已知问题台账维护规则

- **位置**：`references/scenecraft-known-issues.md`
- **何时读**：L2/L3 预审时、改代码前（查目标文件有无已知问题）
- **何时写**：
  - 修好一个问题 → 改 ⬜ 为 ✅，加修复日期
  - 发现新问题 → 追加到对应文件的章节末尾
  - 新增文件首次审计 → 新增章节
- **快速统计**：台账顶部有汇总表，每次更新后同步刷新数字
- **不做的事**：不把台账当"每次必读的长文"——AI 只需读目标文件对应的章节，不需要全部读完

### 8.10 SceneCraft 专属踩坑记录

> 编辑器工具开发特有的坑，与通用坑（7.8）分开记录。

| # | 踩坑场景 | 后果 | 正确做法 |
|---|---------|------|---------|
| 1 | `OnGUI()` 中执行 P4 命令 / 外部进程 | 编辑器卡死 | 必须由按钮点击触发 |
| 2 | GUIStyle 在域重载（Domain Reload）后丢失 | 样式变成空白 | 用 `SceneCraftStyles` 集中缓存 + null 检查 lazy 初始化 |
| 3 | `Bin~` 目录被 P4 trigger 拦截报错 | P4 add 失败 | 过滤带 `~` 后缀的 Unity 临时目录 |
| 4 | P4CLIENT 环境变量默认是机器名 | P4 命令执行在错误的 workspace | 显式传 `-c` 参数或从 p4 set 读取 |
| 5 | 列表"预览 0 文件"问题 | 用户体验差 + 五轮调试 | 显示前验证数据源是否已加载，未加载显示"加载中..." |

> **遇到新坑时，必须追加到此表。这是活文档。**

---

## 九、沟通风格偏好

- **直接高效，不废话** — 不需要"Great question!"之类的客套
- **先动手后沟通** — 遇到问题先尝试解决，边做边说
- **偶尔皮一下但干活不含糊** — 幽默可以有，但不能影响输出质量
- **中文为主** — 回复默认使用中文

---

## 参考文档

- **项目上下文**：`references/project-context.md` — 项目路径、版本历史、P4 集成决策等
- **SceneCraft 架构速查**：`references/scenecraft-architecture.md` — 28 个 .cs 文件的完整架构文档（类声明、核心方法、字段、依赖关系、快速定位指南）。**修改 SceneCraft 代码前必读此文档**，避免遍历全部源码浪费 token
- **SceneCraft 已知问题台账**：`references/scenecraft-known-issues.md` — 代码预审发现的所有问题（含严重等级、行号、修复建议、修复状态）。**改代码前查对应文件章节**，修好了标 ✅，发现新问题追加
- **渲染前沿知识索引**：`references/rendering-knowledge-index.md` — Unity 渲染管线战略、全球前沿技术图谱（三个梯队）、移动端渲染特有知识、Shader 学习资源、知识保鲜策略。**接到渲染相关任务时先读此文档定位方向**
- **游戏开发知识索引**：`references/gamedev-knowledge-index.md` — SO 架构参考模式、反模式观察名单、移动端/小游戏性能知识、竞品研究方向。**接到游戏开发任务时先读此文档**
- **references 目录管理规范**：`references/README.md` — 知识文档的分类规则、命名规范、文档头模板、生命周期管理。**新增 references 文档前必读此规范**
